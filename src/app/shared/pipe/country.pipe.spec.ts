/* tslint:disable:no-unused-variable */

import { CountryPipe } from './country.pipe';

describe('Pipe: Countrye', () => {
  it('create an instance', () => {
    let pipe = new CountryPipe();
    expect(pipe).toBeTruthy();
  });
});
